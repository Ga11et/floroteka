export type plantPropsType = {
    name: string
    description: string
    img: string
    id: string
}
export type beforeAfterPostPropsType = {
    id: string
    heading: string
    date: string
    text: string
    before: string
    after: string
}